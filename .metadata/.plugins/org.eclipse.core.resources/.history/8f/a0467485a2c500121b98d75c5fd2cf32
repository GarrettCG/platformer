#version 330 core

in vec2 UV;

out vec4 color;

uniform sampler2D renderedTexture;
uniform float time;

void main(){
	color = vec4(texture( renderedTexture, UV + 0.00005*vec2( sin(time+100*UV.x),cos(time+100*UV.y)) ).xyz,1) ;
	//color=texture( renderedTexture, UV);
}