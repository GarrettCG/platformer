#version 330 core

in vec2 UV;

out vec4 color;

uniform sampler2D renderedTexture;
uniform float time;

void main(){
	color = vec4(texture( renderedTexture, UV + 0.5*vec2( sin(time+1920.0*UV.x),cos(time+1080.0*UV.y)) ).xyz,1) ;
	//color=texture( renderedTexture, UV);
}